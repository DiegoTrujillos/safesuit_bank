import 'package:flutter/material.dart';

class cardWidget extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      height: 200.0,
      width: 100.0,
      padding: const EdgeInsets.all(10.0),
      child: const Text("hola Mubdo"),);
  }

}